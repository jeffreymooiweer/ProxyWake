# ProxyWake v4.2.2

## Summary

Patch correction release. Aligns repository version references, changelog compare links, and release metadata after the v4.2.1 finalization sprint. No new features and no breaking API changes.

## Added

- `docs/releases/v4.2.2.md` (this file)

## Changed

- All current version references set to **4.2.2**
- `scripts/check_version_consistency.py` uses generic changelog compare-link rules
- CHANGELOG `[Unreleased]` and `[4.2.2]` compare links corrected

## Fixed

- Final release consistency fixes after v4.2.1 finalization
- Version-check no longer hardcodes v4.2.1-specific expectations
- Docker workflow dispatch description updated (tags derived from `backend/version.py`)
- v4.2.2 release/tag aligned with current `main` without rewriting v4.2.1

## Upgrade Notes

No database migration required.
No breaking API changes.

```bash
docker pull jeffersonmouze/proxywake:4.2.2
docker stop proxywake && docker rm proxywake
# Re-run with same volume and environment variables
```

## Verification

- `python3 scripts/check_version_consistency.py` — pass
- `pytest` — pass
- `npm run build` — pass

## Install

```bash
docker pull jeffersonmouze/proxywake:4.2.2
docker pull jeffersonmouze/proxywake:latest
docker pull jeffersonmouze/proxywake:4.2
```

**Full changelog:** [CHANGELOG.md](../../CHANGELOG.md#422---2026-07-06)
